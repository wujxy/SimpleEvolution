# Scientist 入世：设计定案与实现交接（2026-08-25 15:41）

状态：设计已对齐（本轮对话三次纠偏后闭合），实现进行到一半，正要按终态重排。
给 compact 后的自己：**先读本文第三节（定案）和第五节（重建清单），再动手。**

---

## 一、设计宪法（用户原话精神，不可违背）

1. **scientist 是独立完备的 agent 包**（像 claude code）：脱离 simpleevo 也能单独跑
   ——给个目录当世界、给个目标，就能自己读码、做实验、叫助手，干完整轮研究。
2. **一个容器 = 一个世界**：scientist 与助手 claude 同容器、同文件系统、**能力完全相同**
   （bash + 读写）。没有内部沙箱、没有 git、没有快照——mount 就是边界，世界不需要自我管理机制。
3. **助手入容器 = 镜像里装 claude CLI + 凭据，仅此而已**。work() 就是在同一个 /work 上
   叫 claude 干活；fresh 模式 = 世界里/scratch 下开个副本目录，不是另一个容器。
4. **依赖方向永远是 simpleevo → scientist**，scientist 包零 simpleevo import。
   与 simpleevo 沟通的窄腰 = 账本工具（update_research_state / search_experiments /
   inspect_experiment / inspect_originating_research_state）+ 出口契约（deliver/abstain）。
5. **harness 只在两个时刻在场**：开世界（mounts + worktree@node_sha + spec）、
   收世界（出口契约检查 → 快照 → 跑 eval）。**中间 scientist 干了什么一律不能侵入**——
   per-work 快照、审计"助手每步改了什么"、work/consult 配额闸门（HandTally）都是
   旧设计垃圾。看过程只能事后读世界目录里留下的文件。
6. **功能不消失，所有权搬家**：预算/时间 nudge 变成包自己的仪表（agent 拥有自己的循环，
   自己看得见钟）；出口契约（deliver 前须 state 落账）在门口检查。
   墙钟 kill 保留（那是关世界，不是管研究）。
7. **快照 = harness 给世界发身份证**（evolution 图的节点是 SHA；归因/复跑/防伪造），
   出口处对世界目录机械 commit，scientist 包看不见它；单机模式无快照（无裁决者即无身份需求）。
8. **单机账本后端 = 本地文件**（用户已确认：出口契约在单机下成立）。
9. 遇到旧设计垃圾**大大方方去掉，不要为了保留而保留**（用户明确指示）。

## 二、本轮对话的纠偏史（避免再犯）

- 我第一次跑偏：造了"宿主包装层"（scientist/inworld.py）替 scientist 管模型、管助手、
  管落账 → 被纠正：中间人拆掉，scientist 自己带脑子自己带助手。
- 第二次跑偏：想把宿主 hands 的逐次 git 快照搬进容器（gitdir 挂载等）→ 被纠正：
  这是在 world 里再造 sandbox；快照是 harness 出口动作，本来就在原位不用动。
- 第三次跑偏：问"agent 该不该自带脑子"→ 假问题，源自错误架构；独立包当然自带。

## 三、已完成的代码（本 session，全部未提交）

1. **scientist/model.py**：原生 tool-call 通道。`ModelReply.tool_calls: tuple[ToolCall,...]`、
   `ToolCall(id,name,arguments,arguments_raw)`、`complete(..., tools=[...])`（tools 与
   json_object 互斥）、流式 tool_calls 分片组装（`_tool_call_from_fragment`，参数 JSON
   解析失败保 raw）、Anthropic 通道对 tools 快速失败。**双轨**：不传 tools 行为不变。
2. **scientist/native_tools.py**（新）：12 个原生工具 schema（local: bash/read_file/write_file；
   转发: consult/work/update_research_state/search_experiments/inspect_experiment/
   inspect_originating/use_research_skill；终态: deliver_world/abstain）+
   三个 native 提示块（NATIVE_TOOL_BLOCK 优先级声明 / NATIVE_PROTOCOL_BLOCK /
   NATIVE_BOUNDARIES 入世版）+ `native_actions()` + wire 助手。
3. **scripts/probe_prompt.py**：`--native` 单轮探针（上下文同装配，工具走 API payload）。
4. **scientist/scientist.py**：从 research() 提出模块级 `assemble_research_context()`
   （行为保持，291 测试全绿），供宿主侧共用。
5. **scientist/inworld_cli.py**（新，将被溶解）：LocalWorld（bash/读写 + PathBoundary +
   git env 翻译）、StdioRpcChannel、ProxyModel（将删）、ProbeChannel、
   `run_native_episode` 循环（native 版 AgentRuntime：nudges/墙钟余量/cut_off/
   notebook checkpoint/compaction）、probe 模式。
6. **scientist/inworld.py**（新，**垃圾，删**）：宿主包装层，方向错误的产物。
7. **tests/scientist/test_model_native_tools.py**：7 个测试，全过。
8. 全仓 **291 passed**（284 旧 + 7 新）。

**探针实测**（重要证据）：`python -m scientist.inworld_cli --probe`（真实
examples/xsbench_opt/repo、真实模型、native 通道）第一步 = bash + search_experiments
（落地 + 覆盖查询）——seat-v2 纪律在原生通道完好。

## 四、已核实的关键事实（重建要用）

- **模块纯度**（零 simpleevo import，包内可直用）：model.py（SDK import 都在 from_config
  里）、native_tools.py、research_files.py（PathBoundary/ResearchFiles）、
  scientist_session.py（`_load_from_dir(session_dir, prompt_version, episode_id=)` 可直接用）、
  research_skills.py、memory/context.py（build_generation_context）、context.py
  （build_first_layer_pack / build_world_transition_pack）、prompts/（load_semantic）。
  **scientist.py 不纯**（顶层 import simpleevo.research_state）→ 需要的常量要**移植**进新包：
  `_seat_identity_block`/`_SCIENTIST_MODE`、charter 装配、`_COLD_START`、`_BUDGET_NUDGE`、
  `_TIME_NUDGE`、`_SUSPEND_PROMPT`、终态校验规则（handover 三键非空 + 词数软400硬600；
  abstain 必须非空 axes_checked；deliver/abstain 前须 state 落账）。
- **调度器入口**：simpleevo/jobs/base.py:171 `"proposer": "scientist.cli"` —— worker
  搬走时改这一行。
- **claude 调用配方**（scientist/assistant/agent.py，忠实移植）：
  `claude -p --input-format text --output-format stream-json --verbose
  --allowedTools <列表> [--model M] [--effort E]`，prompt 走 stdin，解析 stream-json 的
  result 事件取 text+usage。consult 工具面 `Read,Grep,Glob,WebSearch,WebFetch,Task`
  （无写工具——用 claude 自己的 flag 表达，不是沙箱）；work 工具面
  `Read,Grep,Glob,Edit,Write,Bash,WebSearch,WebFetch`。
- **consult/work 提示词**：hands.py `_CONSULT_PROMPT`/`_WORK_PROMPT`（词帽 300 +
  结尾 fenced JSON digest），`_parse_tail`/`_cap_words` 直接搬。raw 转录存
  世界内 `.scientist/assistant/<call_id>/`。fresh = 副本自 pristine（有 /node 挂载用之，
  否则副本当前世界并注明），用后删。
- **容器**：镜像 python 3.9.25 仅此一个解释器、无 claude → 包必须纯 stdlib 才能入世；
  模型传输用 urllib+SSE 手写。宿主 venv 是 3.12（/datafs/users/wujxy/py_venv/my_env/bin/python）。
  嵌套 apptainer：unset APPTAINER_BIND + --userns。
- **凭据**：deepseek sk- token 在 /home/wujxy/.claude/settings_ds.json.backup
  （ANTHROPIC_AUTH_TOKEN）；OpenAI-compat 用 OPENAI_API_KEY=<同token> +
  OPENAI_BASE_URL=https://api.deepseek.com。researcher 配置见
  examples/xsbench_opt/task-supervisor.yaml（deepseek-v4-flash, reasoning_effort low）。
- **真实世界**：examples/xsbench_opt/repo（纯 clone，.git 是目录，可编译）。

## 五、重建清单（下一步，按序执行）

**新包模块**（零 simpleevo import）：
1. `scientist/model_stdlib.py` — StdlibChatModel：urllib POST {base_url}/chat/completions，
   stream:true，逐行解 SSE（`data: {...}`/`data: [DONE]`），复用 `_RetryChatModel` 重试骨架；
   HTTPError 包成带 `status_code` 的异常（`_is_transient` 认它）；400 时降级去掉
   stream_options；env 回退 OPENAI_API_KEY/OPENAI_BASE_URL。
2. `scientist/world.py` — LocalWorld 从 inworld_cli 搬来（+可选 /node pristine 根）。
3. `scientist/assistant_tools.py` — InWorldAssistant：consult/work 在世界内拉 claude
   （lean 移植：保留提示词/词帽/JSON tail/raw 落盘；删沙箱/快照/tally/DB 行）。
4. `scientist/ledger.py` — Ledger 后端协议（update_research_state / search_experiments /
   inspect_experiment / inspect_originating / state_on_file / note_assistant_call）
   + LocalLedger（session 目录 jsonl：research_state.jsonl、experiments.jsonl、
   assistant_calls.jsonl）+ ChannelLedger（stdio line-JSON：ledger/conclusion/usage）。
5. `scientist/agent.py` — 循环从 inworld_cli 的 run_native_episode 搬来 + 移植 nudge/
   suspend 文本 + **包内出口契约校验**（handover 词帽、abstain axes、state_on_file）。
   use_research_skill 改为 LOCAL（技能文件随包走）。
6. `scientist/cli.py` — **整个替换**为 agent 入口：`--spec spec.json --world DIR
   [--session DIR] [--ledger-channel] [--probe]`；产出 conclusion.json + 本地账本 + session。

**搬迁与删除**：
- 旧 worker 内容（现 scientist/cli.py 全文）→ `simpleevo/jobs/proposer_worker.py`
  （import 相应调整），base.py 一行改 `"proposer": "simpleevo.jobs.proposer_worker"`，
  tests/test_cli.py 跟随。
- **删**：scientist/inworld.py（垃圾）、scientist/inworld_cli.py（内容溶解进
  world.py/agent.py/cli.py）、ProxyModel + model_call RPC。
- **旧 JSON 协议路径**（scientist.py 的 ScientistAgent/orchestrator/ResearchTools A 类/
  宿主 hands）：冻结不新增，作为生产路径保留到新路径过容器冒烟（镜像装 claude 后），
  然后**一刀删**。这是时序不是保留——用户已批准删除，别再围着它搭任何新东西。

**spec.json 草案**：
```json
{"goal":"...","gate_block":"...","editable_paths":[...],"base_sha":"...","lens":{...},"node_id":"...",
 "model":{"api":"openai","model":"...","base_url":"...","api_key":"...","reasoning_effort":"low"},
 "assistant":{"command":"claude","model":null,"effort":null,"node_world":"/node"},
 "budget":{"steps":200,"wall_seconds":3600,"command_timeout_seconds":360,
           "command_output_cap_chars":12000,"consult_timeout_seconds":900,
           "work_default_minutes":30,"distill_word_cap":300},
 "opening_messages":[{"role":"user","content":"..."}],
 "paths":{"work":"/work","repo":"/repo","scratch":"/scratch"}}
```
系统提示装配（包内做）：identity(lens) + charter(prompts/proposer.md 或 spec 覆盖) +
build_generation_context(世界事实) + startup skills + NATIVE_TOOL_BLOCK + 技能目录 +
NATIVE_PROTOCOL_BLOCK + NATIVE_BOUNDARIES + notebook(session) + spec.opening_messages。

**验证顺序**：单机 demo（host 上 scratch xsbench 世界 + 真模型 + 真 claude → 真结论）
→ probe 行为 ≥ seat-v2 基线（对照 runs/probe-a-smoke-4：delivered/+36.8%/consult=1/work=5）
→ 全仓测试 → 镜像装 claude 后容器冒烟 → 切 supervisor → 删旧路径。

## 六、当前 git/杂项

- 未提交累计：seat-v2 移植 + 影子工具清理 + probe 脚本与 variants + 设计文档
  （docs/design/scientist工具分类与入世设计.md，**内容已过时**：其中"宿主包装层/账本通道
  代理/物种 (a) 细节"要按本文宪法改写）+ 本 session 全部新代码。用户说提交要说一声。
- 记忆已存：oneworld-scientist-package.md（宪法要点）、seat-v2-context-rewrite.md。
- 本文件即交接；compact 后从第五节继续。
