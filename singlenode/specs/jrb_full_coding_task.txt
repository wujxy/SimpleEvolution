You are working on JunoResBench white-box electron reconstruction with FULL READOUT in /work: every channel that pulsed in the window is digitized (zero-suppressed full readout — physics-hit channels and dark-noise-only channels alike; silent channels are absent). wf_offsets maps events to adc rows, adc_pmt_ids names each row's channel, and t_run_s timestamps each event in seconds from the start of the continuous run. In the test split, pmt_offsets is absent: the hit-channel identity must come from the waveforms. Improve the solver in src/ — lower energy_res on the labeled val split (ties broken by vertex_res_cm, then timing_res_ns) while keeping the prediction contract valid: `python3 src/solve.py --data <split-dir> --train <split-dir> --out <pred.npz>` writes E_rec, x_rec, y_rec, z_rec, t0_rec (t0 measured from window start), and must also run on the test split, which carries no truth.

Gates you can run yourself:
  bash scripts/check_verify.sh   (VERIFY gate: solver runs on val, emits a well-formed in-range prediction npz)
  bash scripts/bench.sh          (val objective: energy_res/energy_bias/vertex_res_cm/timing_res_ns + SANITY floors)

The complete numpy-only forward model that produced the data ships in benchmarks/whitebox_task_electron_full/ (read its TASK.md): you may read, run and modify it, and generate unlimited labeled events under your own seeds. The test set was produced by exactly this code with an unknown large random seed (absent from the package; brute-force search is not the task).

The detector's photostatistical design floor is ~3%/sqrt(E) energy, ~10 cm vertex, ~1 ns timing with the full readout; the charge-centroid baseline in src/ is far from that. At the exit the harness re-runs your final solver on the held-out test split and scores it against truth you never see.

Measure your own progress with the two scripts; work in /work (generated data and outputs go to /scratch — the package filesystem is read-only). python3 is 3.9 with numpy 2.0 only. The waveform blocks are multi-GB adc.npy — open them memmapped (np.load(..., mmap_mode="r"), or juno_res_bench.split_io.load_split from the package) and work in chunked passes. When you are done, leave the best solver in src/solve.py and finish with a short summary of what you tried and the final val scores.
